#include <stdio.h>
#include <math.h>

int main()
{
    // double r = 0.07, p = 1;
    // int n = 1;
    // while (n <= 10)
    // {
    //     p = p * (1 + r);
    //     n++;
    // }
    // printf("p is %f", p);

    //1.
    // float p21, r = 0.07, n = 10;
    // p21 = pow(1 + r, n);
    // printf("p is %f\n", p21);

    //2.
    // double r1, r2, r3, r4, r5, p, p1, p2, p3, p4, p5;
    // r1 = 0.015;
    // r2 = 0.021;
    // r3 = 0.0275;
    // r5 = 0.03;
    // r4 = 0.0035;
    // p = 1000;

    // p1 = p * (1 + 5 * r5);
    // p2 = p * (1 + r2 * 2) * (1 + r3 * 3);
    // p3 = p * (1 + r3 * 3) * (1 + r2 * 2);
    // p4 = p * pow(1 + r1, 5);
    // p5 = p * pow(1 + r4 / 4, 20);
    // printf("p1 = %-10.2f\n", p1);
    // printf("p2 = %-10.2f\n", p2);
    // printf("p3 = %-10.2f\n", p3);
    // printf("p4 = %-10.2f\n", p4);
    // printf("p5 = %-10.2f\n", p5);

    //3.
    // float d = 300000, p = 6000, r = 0.01, m;
    // m = log10(p / (p - d * r)) / log10(1 + r);
    // printf("m=%6.1f\n", m);

    //5.
    // int a, b;
    // float x, y;
    // char c1, c2;
    // scanf("a=%db=%d", &a, &b);
    // scanf("%f%e", &x, &y);
    // scanf("%c%c", &c1, &c2);
    // printf("a=%d,b=%d,x=%f,y=%f,c1=%c,c2=%c\n", a, b, x, y, c1, c2);

    //6.
    // char c1 = 'C', c2 = 'h', c3 = 'i', c4 = 'n', c5 = 'a';
    // c1 = c1 + 4;
    // c2 = c2 + 4;
    // c3 = c3 + 4;
    // c4 = c4 + 4;
    // c5 = c5 + 4;
    // printf("password is %c%c%c%c%c\n", c1, c2, c3, c4, c5);
    // putchar(c1);

    //7.
    

    return 0;
}